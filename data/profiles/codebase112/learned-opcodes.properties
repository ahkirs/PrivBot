#Runtime-learned opcode mappings - do not edit manually
#Cleared 2026-05-17 after audit: OpcodeCaptureService had stored NPC identifier
#values as opcodes (NPC_SECOND=745 — 745 was the captured NPC's array index; WALK=516 —
#516 was a player-click capture for an NPC attack labelled NPC_THIRD by MenuRuleDeriver,
#NOT a walk opcode). Authoritative values are now pinned in opcode-mapping.properties
#which has priority via OpcodeMapping.getServerOpcode(). Re-learning is allowed but the
#static map will continue to override for pinned categories.
